import React from 'react'
import "./CustomeToastContainer.css"
const CustomToastContainer = ({children}) => {
  return (
    <div className='Custom'>{children}</div>
  )
}

export default CustomToastContainer